# Application Configuration
spring.application.name=crm-backend
server.port=8080

# Database Configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/crm_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:crm_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:crm_password}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Flyway Configuration
spring.flyway.enabled=${FLYWAY_ENABLED:false}
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=1
spring.flyway.locations=classpath:db/migration

# Logging
logging.level.org.springframework.web=INFO
logging.level.org.hibernate=INFO
logging.level.com.crm=DEBUG

# Server Configuration
server.error.include-message=always
server.error.include-binding-errors=always

# Email Configuration - IMAP (Receiving)
email.imap.host=mail.q-prospect.pl
email.imap.port=993
email.imap.username=crm@qprospect.pl
email.imap.password=ssd51eGurVa
email.imap.protocol=imaps
email.imap.ssl.enable=true
email.imap.ssl.trust=*

# Email Configuration - SMTP (Sending)
spring.mail.host=mail.q-prospect.pl
spring.mail.port=587
spring.mail.username=crm@qprospect.pl
spring.mail.password=ssd51eGurVa
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=mail.q-prospect.pl
spring.mail.from=crm@qprospect.pl

# Email Fetching Configuration
email.fetch.enabled=true
email.fetch.interval=300000
email.fetch.folder=INBOX

# AI Classification (using Groq - free API)
ai.classification.enabled=true
ai.api.url=https://api.groq.com/openai/v1/chat/completions
ai.api.key=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

# Woodpecker API Configuration
woodpecker.api.base-url=https://api.woodpecker.co/rest
woodpecker.api.key=555072.7c122c4721c17457976edac80ac6eacc0929c72445da6d5837b115830cf5d01f
woodpecker.api.enabled=true
